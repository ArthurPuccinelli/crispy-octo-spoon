# Supabase Configuration (REQUIRED for production)
# Get these from your Supabase project: Settings > API
NEXT_PUBLIC_SUPABASE_URL=sua_url_do_supabase_aqui
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anonima_do_supabase_aqui

# API Local (mantido para referência)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Runtime environment (used by Netlify/Next)
# development | production
NODE_ENV=development

# Optional: Public site URL (for Netlify and other hosts)
# Set automatically on Netlify as URL / DEPLOY_URL
NEXT_PUBLIC_SITE_URL=

# IMPORTANT: For Netlify deployment, set these environment variables in:
# Netlify Dashboard > Site Settings > Environment Variables
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
