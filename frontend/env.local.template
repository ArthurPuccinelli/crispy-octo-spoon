# ===========================================
# CONFIGURAÇÕES PARA DESENVOLVIMENTO LOCAL
# ===========================================
# Este arquivo contém todas as variáveis necessárias para testes locais
# sem impactar a produção no Netlify

# ===========================================
# SUPABASE (OBRIGATÓRIO para funcionalidades básicas)
# ===========================================
# Obtenha estes valores em: Supabase Dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=sua_url_do_supabase_aqui
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anonima_do_supabase_aqui

# Para as funções Netlify (usar os mesmos valores)
SUPABASE_URL=sua_url_do_supabase_aqui
SUPABASE_ANON_KEY=sua_chave_anonima_do_supabase_aqui

# ===========================================
# DOCUSIGN (OPCIONAL - apenas se testar integração DocuSign)
# ===========================================
# Configurações DocuSign (nomes atualizados)
DOCUSIGN_ACCOUNT_ID=your_account_id_here
DOCUSIGN_USER_ID=your_user_id_here
DOCUSIGN_IK=your_integration_key_here

# Uma das duas formas de prover a chave privada:
# 1) Base64 do PEM completo (recomendado)
DOCUSIGN_RSA_PEM_AS_BASE64=base64_of_private_pem
# 2) PEM em texto (com \n escapado) - descomente se usar esta opção
# DOCUSIGN_RSA_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----

# Base path do OAuth/Auth server
DOCUSIGN_AUTH_SERVER=account-d.docusign.com
# Opcional: caminho REST base (fallback)
DOCUSIGN_BASE_PATH=https://demo.docusign.net/restapi

# Escopos para JWT Grant
DOCUSIGN_IAM_SCOPES=signature,impersonation,aow_manage

# DocuSign Maestro (beta)
DOCUSIGN_MAESTRO_BASE_URL=https://api-d.docusign.net/maestro/v1

# ===========================================
# CONFIGURAÇÕES DE DESENVOLVIMENTO
# ===========================================
# Runtime environment
NODE_ENV=development

# API Local (para desenvolvimento)
NEXT_PUBLIC_API_URL=http://localhost:3001

# URL do site local (opcional)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ===========================================
# CONFIGURAÇÕES DE LOGGING (OPCIONAL)
# ===========================================
LOG_LEVEL=debug
ENABLE_AUDIT_LOG=true
LOG_DOCUSIGN_REQUESTS=true
ENABLE_QUERY_LOG=true

# ===========================================
# CONFIGURAÇÕES DE SEGURANÇA (OPCIONAL)
# ===========================================
ENABLE_RATE_LIMITING=false
MAX_REQUESTS_PER_MINUTE=1000
ENABLE_CORS=true
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://*.netlify.app

# ===========================================
# CONFIGURAÇÕES DE CACHE (OPCIONAL)
# ===========================================
ENABLE_CACHE=false
CACHE_TTL=300

# ===========================================
# INSTRUÇÕES DE USO
# ===========================================
# 1. Copie este arquivo para .env.local
# 2. Substitua os valores marcados com "sua_*_aqui" pelos valores reais
# 3. Para testes básicos (sem DocuSign), apenas configure as variáveis do Supabase
# 4. Para testes completos, configure também as variáveis do DocuSign
# 5. Execute: npm run dev
